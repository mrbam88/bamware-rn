import { FC } from "react"
import { View } from "react-native"
import { Text } from "@/components"

interface SettingsScreenProps {}

export const SettingScreen: FC<SettingsScreenProps> = () => {
  return (
    <View>
      <Text>Setting Screen</Text>
    </View>
  )
}
